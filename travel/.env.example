# WanderWise — copy to .env and fill from the NEW dedicated Supabase project
# (Project Settings -> API). Never commit the real .env.
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
# service_role key - SERVER ONLY (audit logging, account deletion).
SUPABASE_SERVICE_ROLE_KEY=

# Phase 2+ (leave blank for now)
ANTHROPIC_API_KEY=
GOOGLE_PLACES_API_KEY=
